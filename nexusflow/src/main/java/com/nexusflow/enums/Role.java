package com.nexusflow.enums;

/**
 * Papel base do usuário no sistema.
 * MANAGER = dono do comércio (criado pelo sistema).
 * EMPLOYEE = qualquer colaborador (incluindo subgerentes).
 */
public enum Role { MANAGER, EMPLOYEE, SUPER_ADMIN }
